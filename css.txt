/* ============================================================= */
/* 1. إعادة ضبط الألوان الفخمة واللوحة الليلية المريحة           */
/* ============================================================= */
:root {
    --primary-color: #c9a063;
    --bg-dark: #1a1e24;
    --bg-dark-card: #222831;
    --border-dark: rgba(255, 255, 255, 0.08);
    --text-dark: #f0f2f5;
    --bg-light: #f8f9fa;
    --text-light: #212529;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Cairo', 'Amiri', sans-serif;
    transition: background 0.3s, color 0.3s;
}

body.dark {
    background-color: var(--bg-dark);
    color: var(--text-dark);
}

body.light-mode {
    background-color: var(--bg-light);
    color: var(--text-light);
}

/* شريط تقدم القراءة */
#reading-progress {
    position: fixed;
    top: 0;
    left: 0;
    height: 3px;
    background: var(--primary-color);
    width: 0%;
    z-index: 10001;
    transition: width 0.1s ease-out;
}

/* زر العودة للأعلى */
#backToTopBtn {
    position: fixed;
    bottom: 30px;
    left: 30px;
    background: var(--primary-color);
    color: #1a1e24;
    width: 45px;
    height: 45px;
    border-radius: 50%;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.1rem;
    box-shadow: 0 4px 10px rgba(0,0,0,0.3);
    transition: transform 0.3s, opacity 0.3s;
    opacity: 0;
    visibility: hidden;
    z-index: 9999;
}

#backToTopBtn.show {
    opacity: 1;
    visibility: visible;
}

#backToTopBtn:hover {
    transform: translateY(-3px);
}

/* ============================================================= */
/* 2. الهيدر العلوي وأشرطة التنقل                                */
/* ============================================================= */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 5%;
    background: rgba(26, 30, 36, 0.85);
    border-bottom: 1px solid var(--border-dark);
    backdrop-filter: blur(10px);
    position: sticky;
    top: 0;
    z-index: 1000;
}

body.light-mode header {
    background: rgba(255, 255, 255, 0.9);
    border-bottom: 1px solid #dee2e6;
}

.logo {
    font-family: 'Amiri', 'Cairo', serif;
    font-size: 2.2rem;
    font-weight: 700;
    color: var(--primary-color);
    letter-spacing: 1px;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    transition: color 0.3s ease, transform 0.3s ease;
    cursor: pointer;
}

.logo:hover {
    color: #dfbe8a;
    transform: scale(1.02);
}

.glass-tools {
    display: flex;
    gap: 8px;
    align-items: center;
}

.lang-pill {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--border-dark);
    color: inherit;
    padding: 5px 12px;
    border-radius: 20px;
    cursor: pointer;
    font-size: 0.85rem;
}

.menu {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(0, 0, 0, 0.15);
    padding: 10px 5%;
    border-bottom: 1px solid var(--border-dark);
    gap: 20px;
}

.sections {
    display: flex;
    align-items: center;
    gap: 8px;
    overflow-x: auto;
    white-space: nowrap;
    flex-grow: 1;
    scrollbar-width: none;
}

.category-btn {
    color: inherit;
    text-decoration: none;
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 0.88rem;
    font-weight: 600;
    border: 1px solid rgba(255, 255, 255, 0.05);
    background: rgba(255, 255, 255, 0.02);
}

.category-btn.active, .category-btn:hover {
    background: var(--primary-color) !important;
    color: #1a1e24 !important;
    border-color: var(--primary-color) !important;
}

/* ============================================================= */
/* 3. شبكة عرض كروت المقالات                                      */
/* ============================================================= */
.main-content { padding: 40px 5%; }

.news-grid-flexible {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(290px, 1fr));
    gap: 22px;
}

.story-card {
    position: relative;
    display: flex;
    flex-direction: column;
    height: 320px;
    background: var(--bg-dark-card);
    border: 1px solid var(--border-dark);
    border-radius: 12px;
    overflow: hidden;
}

.story-thumbnail {
    position: absolute;
    width: 100%;
    height: 100%;
}

.story-thumbnail img { width: 100%; height: 100%; object-fit: cover; }

.story-overlay-content {
    position: absolute;
    bottom: 0;
    padding: 20px;
    width: 100%;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.9), transparent);
}

/* ============================================================= */
/* 4. المقالات الداخلية والتعليقات                                 */
/* ============================================================= */
.article-container { max-width: 800px; margin: 40px auto; padding: 0 20px; }

.article-text {
    font-family: 'Amiri', serif;
    font-size: 1.3rem;
    line-height: 1.8;
    text-align: justify;
}

.article-text p:first-of-type::first-letter {
    font-size: 3.2rem;
    color: var(--primary-color);
    float: right;
    margin-left: 12px;
}

.article-actions {
    display: flex;
    gap: 12px;
    padding: 15px 0;
    border-top: 1px solid var(--border-dark);
    border-bottom: 1px solid var(--border-dark);
}

.comments-section {
    background: var(--bg-dark-card);
    padding: 25px;
    border-radius: 12px;
}

.comment-form textarea { width: 100%; min-height: 110px; padding: 15px; margin-top: 15px; }

.submit-comment-btn {
    background: var(--primary-color);
    color: #1a1e24;
    padding: 12px 25px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
}
